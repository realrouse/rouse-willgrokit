# Moved

Active code lives in **[realrouse/lbry-rs](https://github.com/realrouse/lbry-rs)**.

That workspace includes:

- `lbry-blob` / `lbry-blob-iroh` crates  
- download **and upload** (PutBlob) superpeer  
- CLI + **web** upload via companion  

This folder remains as the historical first MVP snapshot from research.
